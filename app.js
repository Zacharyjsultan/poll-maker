// import functions and grab DOM elements
const questionInputEl = document.getElementById('question-input');
const optionAInputEl = document.getElementById('option-a-input');
const optionBInputEl = document.getElementById('option-b-input');

const optionAYesEl = document.getElementById('option-a-yes');
const optionBYesEl = document.getElementById('option-b-yes');

const optionANoEl = document.getElementById('option-a-no');
const optionBNoEl = document.getElementById('option-b-no');

const addPollEl = document.querySelector('#add-poll');
const archivePollEl = document.querySelector('#archive-poll');
// let state

// set event listeners 
  // get user input
  // use user input to update state 
  // update DOM to reflect the new state
